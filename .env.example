# Copy to .env on the Droplet (or anywhere you run `docker compose up`).
# Comma-separated frontend origins, no trailing slash.
TRIPWE_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
